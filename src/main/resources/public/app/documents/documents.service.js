'use strict';

var documentsModule = require('./_index.js');

function documentsService($q, $http, Upload, $cookies, APP_SETTINGS, globalService) {
    var apiUrl = APP_SETTINGS.apiUrl;
    var service = {};

     return service;
}

documentsModule.service('documentsService', documentsService);
